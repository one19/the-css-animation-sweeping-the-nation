import { Column, Row } from '../Column';
import ClickReveal from '../ClickReveal';
import Puzzle from '../Puzzle';

# Standard JS Approach

<Row>
<Column>

JS method involves nested maps, setTimeout and state management:

```tsx
cells.map((row, y) =>
  row.map((cell, x) => {
    setTimeout(() => animateCell(x, y), (Math.abs(x - sx) + Math.abs(y - sy)) * DELAY_IN_MS);
  })
);
```
</Column>
<Column>
<ClickReveal>
- waaaaait what's that `animateCell` function?
- so we're setting dom properties on the children yeah?
- what happens if they rerender?
- what happens if they unmount?
- what happens if we unmount?
- ## OH NO TESTING THIS IS GOING TO SUCK
</ClickReveal>
</Column>
</Row>
