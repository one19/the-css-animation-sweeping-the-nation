import { Column, Row } from '../Column';
import ClickReveal from '../ClickReveal';
import Puzzle from '../Puzzle';

# Typical JS Approach

<Row>
<Column>

Common JS method involves nested maps, setTimeout and state management:

```tsx
cells.map((row, y) =>
  row.map((cell, x) => {
    setTimeout(() => animateCell(x, y), (Math.abs(x - sx) + Math.abs(y - sy)) * 200);
  })
);
```
</Column>
<Column>
<ClickReveal>
- waaaaait what's that `animateCell` function?
- Would this be done in a useEffect? What happens when we reset?
- Does that mean all the children will rerender?
- Oh no, those props will have to go all the way down!
- Ohnnononono
- Oh dear
</ClickReveal>
</Column>
</Row>
